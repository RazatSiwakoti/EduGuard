import { Mail, X } from "lucide-react";
import type { AlertPreview, QueueItem } from "../../types/alerts";

interface Props { recipients: QueueItem[]; preview: AlertPreview | undefined; isPreviewLoading: boolean; isSending: boolean; dryRun: boolean; onConfirm: () => void; onCancel: () => void; }
export default function BulkSendDialog({ recipients, preview, isPreviewLoading, isSending, dryRun, onConfirm, onCancel }: Props) {
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-stone-900/40 p-4" role="presentation"><div className="w-full max-w-xl rounded-2xl bg-white p-5 shadow-2xl" role="dialog" aria-modal="true" aria-label="Confirm sending alerts"><header className="flex justify-between"><div><h2 className="text-lg font-semibold">Send {recipients.length} alerts?</h2><p className="text-xs text-stone-500">This cannot be undone once sent.</p></div><button type="button" onClick={onCancel} disabled={isSending} aria-label="Cancel"><X /></button></header>{dryRun && <p className="my-4 rounded-xl bg-amber-50 p-3 text-xs text-amber-900">Dry run: messages will be written to disk.</p>}<ul className="my-4 max-h-40 space-y-1 overflow-auto rounded-xl border p-3 text-sm">{recipients.map((item) => <li key={`${item.student_id}-${item.unit_id}`}>{item.name} <span className="text-stone-500">{item.email}</span></li>)}</ul><p className="text-xs font-medium text-stone-500">Preview</p>{isPreviewLoading || !preview ? <p className="p-5 text-sm text-stone-500">Loading preview...</p> : <pre className="my-2 max-h-48 overflow-auto whitespace-pre-wrap rounded-xl bg-stone-50 p-3 text-xs">{preview.subject}{"\n\n"}{preview.body}</pre>}<footer className="flex justify-end gap-2"><button type="button" onClick={onCancel} disabled={isSending} className="rounded-xl border px-4 py-2 text-sm">Cancel</button><button type="button" onClick={onConfirm} disabled={isSending} className="inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2 text-sm text-white"><Mail className="h-4 w-4" />Send alerts</button></footer></div></div>;
}
